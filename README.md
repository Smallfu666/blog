# blog

Personal blog — database / storage systems.

Built with [Quarto](https://quarto.org/), hosted on [GitHub Pages](https://smallfu666.github.io/blog/).

## Writing a new post

```bash
quarto render   # preview locally
git add -A && git commit -m "..." && git push
```

GitHub Pages auto-deploys from the `docs/` directory on `main`.
